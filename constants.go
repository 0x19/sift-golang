//
//

package sift

import "time"

const (
	API_URL     = "https://api.siftscience.com"
	TIMEOUT     = time.Duration(2.0)
	VERSION     = "0.0.1"
	API_VERSION = 203

	ENV_PRODUCTION = "production"
	ENV_SANDBOX    = "sandbox"
)
